# 中文使用说明

## 触发词与调用方式

当用户提出“制作简历”“把履历整理成 HTML/PDF”“按岗位描述定制简历”“润色简历”“导出一页 PDF”“检查简历是否符合 A4 一页”或“把 Kami 的证据写作方法用于简历”时调用本 Skill。也可以显式指定：`使用 lvsea-resume 生成一份一页 A4 简历`。

## 输入

- 用户事实、旧简历或履历库，优先整理为 `raw.data.yaml`。
- 目标岗位描述、目标语言、联系人信息、作品链接和希望使用的官方模板。
- 多来源事实必须保留来源、确认状态和待核实项；不能用推测补齐数字、职责或成果。

## 推荐工作流

1. 建立 `raw.data.yaml`，保留完整履历和来源。
2. 针对目标岗位复制为 `resume.data.yaml`，只在副本中筛选和润色。
3. 从 12 个官方模板中选择一个，默认 `basic-a4`，只编辑输出目录中的模板副本。
4. 按“角色 + 行动 + 影响”改写证据，校准 ownership，执行 QA 和新鲜招聘者阅读复核。
5. 交付可编辑 HTML、PDF、截图和验证结果；一页 A4、敏感词和模板指纹检查未通过时不得交付。

## 典型应用场景

1. **从事实生成简历**：用户提供教育、工作、项目和技能，建立履历库并生成一页 A4 HTML/PDF。
2. **按岗位定制**：用户提供 JD，从同一份 `raw.data.yaml` 复制出岗位专用 `resume.data.yaml`，调整摘要、项目顺序和技能表达。
3. **迁移旧简历**：用户提供旧 HTML、PDF 或 YAML，保留已确认事实，迁移到 12 个官方模板之一并进行一页排版 QA。
4. **审查与导出**：用户已有简历草稿，需要检查页数、A4 纵向、字体、间距、敏感信息和模板契约，再导出 PDF。

## 产物与验证

主要产物是 `resume.data.yaml`、模板副本中的 `resume.html`、导出的 `resume.pdf`、截图和 QA 报告。至少运行项目已有的 `scripts/export_and_qa.py` 与 `scripts/check-template-id.mjs`；若本机没有 `pdfinfo`、Poppler 或浏览器，必须明确标记缺失验证，不得假称通过。

## 注意事项、限制与边界

- 只使用 12 个官方模板；不得凭空新建 HTML 或把 Kami 的两页模板、构建系统复制进来。
- 一页 A4 是默认硬门槛；用户明确要求保留全部内容时，不得静默删减，应该停下并询问取舍。
- 没有来源或用户确认的数字、职责、指标和所有权只能标为待核实或不写入；不能编造量化成果。
- `raw.data.yaml` 是履历库，不应直接打印或泄露；交付前检查敏感信息和内部链接。
- Kami 只提供已采纳的来源核对、claim ledger、ownership 校准和 recruiter-read 方法；它不改变本 Skill 的一页、12 模板和 QA 契约。
