# Examples

- `demo.data.yaml` — human-copyable resume facts. Copy, edit, paste to an AI coding agent.
- `demo/` — official `basic-a4` template filled from `demo.data.yaml`. Open `demo/resume.html` in a browser; it is that render, not a third layout.
